# UI Studio (canonical read)

This file is the **entry point** named in `project-execution-rules.md` Phase 0 (`ui-studio.md`).

- **Nebula UI Studio** persisted prompt + code live in **`nebula-ui-studio.md`** (`NEBULA_UI_STUDIO_PROMPT` / `NEBULA_UI_STUDIO_CODE`) and under `nebulla-sysh-ui-sysh-studio/approved/` when applicable.
- **v0 (Vercel)** produces the **first** automatic UI generation per project (per execution rules). Refine in **Nebula UI Studio** with Grok 4.1 afterward.
- **Pencil.dev** is deprecated for new Nebula work — use v0 for initial UI per project execution rules.

Read `nebula-ui-studio.md` immediately after this stub for full workflow detail.
