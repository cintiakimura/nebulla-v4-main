# UI generation context (seed pointer)

**Canonical file:** `nebulla-project/ui-generation-context.md` (repo root).

Per-cycle context is written at runtime under the active workspace’s `nebulla-project/` folder. Template prose belongs in the repo-root Guardian doc.
