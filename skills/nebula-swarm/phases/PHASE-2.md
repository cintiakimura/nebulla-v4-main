# Phase 2 – User Interface: Competitor Baseline Analysis (Swarm assist)

**Law:** `nebula-project/project-execution-rules.md` — **Phase 2**: **before** generating UI, analyze **top competitors**; extract common patterns (layout, hierarchy, buttons, color, spacing, navigation); use as baseline; **first** UI generation uses **v0 (Vercel)** once per project; subsequent refinement in **Nebula UI Studio** with Grok 4.1 and **`nebula-ui-studio.md`**. Do **not** use Pencil.dev for new work.

Phase definition and order are **unchanged** (competitor work precedes UI generation).

---

## Grok responsibilities (unchanged)

- Own competitor analysis in product flow.
- Drive v0 for the initial generation pass, then iterate in Nebula UI Studio per mandatory stack (React, Tailwind, shadcn/ui, Lucide — no alternate UI stacks per rules).

---

## Parallel support bundle

- **Quality Agent** — manual only (**Run and Test**); code review + test suggestions on changed files. No parallel Planner/Researcher for new work.

---

## Isolator

No UI code from support agents. `ORCHESTRATOR.md`.
